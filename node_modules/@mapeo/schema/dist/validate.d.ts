import { MapeoValue, FilterBySchemaName, SchemaName } from './types.js';
import * as validations from './validations.js';
import { type DefinedError } from 'ajv';
interface ValidateFunction {
    <TSchemaName extends Extract<keyof typeof validations, SchemaName>>(schemaName: TSchemaName, obj: unknown): obj is FilterBySchemaName<MapeoValue, TSchemaName>;
    errors: null | DefinedError[];
}
declare const validate: ValidateFunction;
export { validate };
