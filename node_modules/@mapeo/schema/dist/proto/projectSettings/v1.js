/* eslint-disable */
import _m0 from "protobufjs/minimal.js";
import { Common_1 } from "../common/v1.js";
import { Timestamp } from "../google/protobuf/timestamp.js";
export const protobufPackage = "mapeo";
function createBaseProjectSettings_1() {
    return {
        common: undefined,
        defaultPresets: undefined,
        configMetadata: undefined,
        name: undefined,
        isInitialProject: false,
    };
}
export const ProjectSettings_1 = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.common !== undefined) {
            Common_1.encode(message.common, writer.uint32(10).fork()).ldelim();
        }
        if (message.defaultPresets !== undefined) {
            ProjectSettings_1_DefaultPresets.encode(message.defaultPresets, writer.uint32(18).fork()).ldelim();
        }
        if (message.configMetadata !== undefined) {
            ProjectSettings_1_ConfigMetadata.encode(message.configMetadata, writer.uint32(26).fork()).ldelim();
        }
        if (message.name !== undefined) {
            writer.uint32(34).string(message.name);
        }
        if (message.isInitialProject === true) {
            writer.uint32(40).bool(message.isInitialProject);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseProjectSettings_1();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.common = Common_1.decode(reader, reader.uint32());
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.defaultPresets = ProjectSettings_1_DefaultPresets.decode(reader, reader.uint32());
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.configMetadata = ProjectSettings_1_ConfigMetadata.decode(reader, reader.uint32());
                    continue;
                case 4:
                    if (tag !== 34) {
                        break;
                    }
                    message.name = reader.string();
                    continue;
                case 5:
                    if (tag !== 40) {
                        break;
                    }
                    message.isInitialProject = reader.bool();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            common: isSet(object.common) ? Common_1.fromJSON(object.common) : undefined,
            defaultPresets: isSet(object.defaultPresets)
                ? ProjectSettings_1_DefaultPresets.fromJSON(object.defaultPresets)
                : undefined,
            configMetadata: isSet(object.configMetadata)
                ? ProjectSettings_1_ConfigMetadata.fromJSON(object.configMetadata)
                : undefined,
            name: isSet(object.name) ? String(object.name) : undefined,
            isInitialProject: isSet(object.isInitialProject) ? Boolean(object.isInitialProject) : false,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.common !== undefined) {
            obj.common = Common_1.toJSON(message.common);
        }
        if (message.defaultPresets !== undefined) {
            obj.defaultPresets = ProjectSettings_1_DefaultPresets.toJSON(message.defaultPresets);
        }
        if (message.configMetadata !== undefined) {
            obj.configMetadata = ProjectSettings_1_ConfigMetadata.toJSON(message.configMetadata);
        }
        if (message.name !== undefined) {
            obj.name = message.name;
        }
        if (message.isInitialProject === true) {
            obj.isInitialProject = message.isInitialProject;
        }
        return obj;
    },
    create(base) {
        return ProjectSettings_1.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b;
        const message = createBaseProjectSettings_1();
        message.common = (object.common !== undefined && object.common !== null)
            ? Common_1.fromPartial(object.common)
            : undefined;
        message.defaultPresets = (object.defaultPresets !== undefined && object.defaultPresets !== null)
            ? ProjectSettings_1_DefaultPresets.fromPartial(object.defaultPresets)
            : undefined;
        message.configMetadata = (object.configMetadata !== undefined && object.configMetadata !== null)
            ? ProjectSettings_1_ConfigMetadata.fromPartial(object.configMetadata)
            : undefined;
        message.name = (_a = object.name) !== null && _a !== void 0 ? _a : undefined;
        message.isInitialProject = (_b = object.isInitialProject) !== null && _b !== void 0 ? _b : false;
        return message;
    },
};
function createBaseProjectSettings_1_DefaultPresets() {
    return { point: [], area: [], vertex: [], line: [], relation: [] };
}
export const ProjectSettings_1_DefaultPresets = {
    encode(message, writer = _m0.Writer.create()) {
        for (const v of message.point) {
            writer.uint32(10).bytes(v);
        }
        for (const v of message.area) {
            writer.uint32(18).bytes(v);
        }
        for (const v of message.vertex) {
            writer.uint32(26).bytes(v);
        }
        for (const v of message.line) {
            writer.uint32(34).bytes(v);
        }
        for (const v of message.relation) {
            writer.uint32(42).bytes(v);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseProjectSettings_1_DefaultPresets();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.point.push(reader.bytes());
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.area.push(reader.bytes());
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.vertex.push(reader.bytes());
                    continue;
                case 4:
                    if (tag !== 34) {
                        break;
                    }
                    message.line.push(reader.bytes());
                    continue;
                case 5:
                    if (tag !== 42) {
                        break;
                    }
                    message.relation.push(reader.bytes());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            point: Array.isArray(object === null || object === void 0 ? void 0 : object.point) ? object.point.map((e) => Buffer.from(bytesFromBase64(e))) : [],
            area: Array.isArray(object === null || object === void 0 ? void 0 : object.area) ? object.area.map((e) => Buffer.from(bytesFromBase64(e))) : [],
            vertex: Array.isArray(object === null || object === void 0 ? void 0 : object.vertex) ? object.vertex.map((e) => Buffer.from(bytesFromBase64(e))) : [],
            line: Array.isArray(object === null || object === void 0 ? void 0 : object.line) ? object.line.map((e) => Buffer.from(bytesFromBase64(e))) : [],
            relation: Array.isArray(object === null || object === void 0 ? void 0 : object.relation) ? object.relation.map((e) => Buffer.from(bytesFromBase64(e))) : [],
        };
    },
    toJSON(message) {
        var _a, _b, _c, _d, _e;
        const obj = {};
        if ((_a = message.point) === null || _a === void 0 ? void 0 : _a.length) {
            obj.point = message.point.map((e) => base64FromBytes(e));
        }
        if ((_b = message.area) === null || _b === void 0 ? void 0 : _b.length) {
            obj.area = message.area.map((e) => base64FromBytes(e));
        }
        if ((_c = message.vertex) === null || _c === void 0 ? void 0 : _c.length) {
            obj.vertex = message.vertex.map((e) => base64FromBytes(e));
        }
        if ((_d = message.line) === null || _d === void 0 ? void 0 : _d.length) {
            obj.line = message.line.map((e) => base64FromBytes(e));
        }
        if ((_e = message.relation) === null || _e === void 0 ? void 0 : _e.length) {
            obj.relation = message.relation.map((e) => base64FromBytes(e));
        }
        return obj;
    },
    create(base) {
        return ProjectSettings_1_DefaultPresets.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c, _d, _e;
        const message = createBaseProjectSettings_1_DefaultPresets();
        message.point = ((_a = object.point) === null || _a === void 0 ? void 0 : _a.map((e) => e)) || [];
        message.area = ((_b = object.area) === null || _b === void 0 ? void 0 : _b.map((e) => e)) || [];
        message.vertex = ((_c = object.vertex) === null || _c === void 0 ? void 0 : _c.map((e) => e)) || [];
        message.line = ((_d = object.line) === null || _d === void 0 ? void 0 : _d.map((e) => e)) || [];
        message.relation = ((_e = object.relation) === null || _e === void 0 ? void 0 : _e.map((e) => e)) || [];
        return message;
    },
};
function createBaseProjectSettings_1_ConfigMetadata() {
    return { name: "", buildDate: undefined, importDate: undefined, fileVersion: "" };
}
export const ProjectSettings_1_ConfigMetadata = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.name !== "") {
            writer.uint32(10).string(message.name);
        }
        if (message.buildDate !== undefined) {
            Timestamp.encode(toTimestamp(message.buildDate), writer.uint32(18).fork()).ldelim();
        }
        if (message.importDate !== undefined) {
            Timestamp.encode(toTimestamp(message.importDate), writer.uint32(26).fork()).ldelim();
        }
        if (message.fileVersion !== "") {
            writer.uint32(34).string(message.fileVersion);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseProjectSettings_1_ConfigMetadata();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.name = reader.string();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.buildDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.importDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
                    continue;
                case 4:
                    if (tag !== 34) {
                        break;
                    }
                    message.fileVersion = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            name: isSet(object.name) ? String(object.name) : "",
            buildDate: isSet(object.buildDate) ? String(object.buildDate) : undefined,
            importDate: isSet(object.importDate) ? String(object.importDate) : undefined,
            fileVersion: isSet(object.fileVersion) ? String(object.fileVersion) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.name !== "") {
            obj.name = message.name;
        }
        if (message.buildDate !== undefined) {
            obj.buildDate = message.buildDate;
        }
        if (message.importDate !== undefined) {
            obj.importDate = message.importDate;
        }
        if (message.fileVersion !== "") {
            obj.fileVersion = message.fileVersion;
        }
        return obj;
    },
    create(base) {
        return ProjectSettings_1_ConfigMetadata.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c, _d;
        const message = createBaseProjectSettings_1_ConfigMetadata();
        message.name = (_a = object.name) !== null && _a !== void 0 ? _a : "";
        message.buildDate = (_b = object.buildDate) !== null && _b !== void 0 ? _b : undefined;
        message.importDate = (_c = object.importDate) !== null && _c !== void 0 ? _c : undefined;
        message.fileVersion = (_d = object.fileVersion) !== null && _d !== void 0 ? _d : "";
        return message;
    },
};
const tsProtoGlobalThis = (() => {
    if (typeof globalThis !== "undefined") {
        return globalThis;
    }
    if (typeof self !== "undefined") {
        return self;
    }
    if (typeof window !== "undefined") {
        return window;
    }
    if (typeof global !== "undefined") {
        return global;
    }
    throw "Unable to locate global object";
})();
function bytesFromBase64(b64) {
    if (tsProtoGlobalThis.Buffer) {
        return Uint8Array.from(tsProtoGlobalThis.Buffer.from(b64, "base64"));
    }
    else {
        const bin = tsProtoGlobalThis.atob(b64);
        const arr = new Uint8Array(bin.length);
        for (let i = 0; i < bin.length; ++i) {
            arr[i] = bin.charCodeAt(i);
        }
        return arr;
    }
}
function base64FromBytes(arr) {
    if (tsProtoGlobalThis.Buffer) {
        return tsProtoGlobalThis.Buffer.from(arr).toString("base64");
    }
    else {
        const bin = [];
        arr.forEach((byte) => {
            bin.push(String.fromCharCode(byte));
        });
        return tsProtoGlobalThis.btoa(bin.join(""));
    }
}
function toTimestamp(dateStr) {
    const date = new Date(dateStr);
    const seconds = date.getTime() / 1000;
    const nanos = (date.getTime() % 1000) * 1000000;
    return { seconds, nanos };
}
function fromTimestamp(t) {
    let millis = (t.seconds || 0) * 1000;
    millis += (t.nanos || 0) / 1000000;
    return new Date(millis).toISOString();
}
function isSet(value) {
    return value !== null && value !== undefined;
}
