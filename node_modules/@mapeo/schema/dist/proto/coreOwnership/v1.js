/* eslint-disable */
import _m0 from "protobufjs/minimal.js";
import { Common_1 } from "../common/v1.js";
export const protobufPackage = "mapeo";
function createBaseCoreOwnership_1() {
    return {
        common: undefined,
        authCoreId: Buffer.alloc(0),
        configCoreId: Buffer.alloc(0),
        dataCoreId: Buffer.alloc(0),
        blobCoreId: Buffer.alloc(0),
        blobIndexCoreId: Buffer.alloc(0),
        coreSignatures: undefined,
        identitySignature: Buffer.alloc(0),
    };
}
export const CoreOwnership_1 = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.common !== undefined) {
            Common_1.encode(message.common, writer.uint32(10).fork()).ldelim();
        }
        if (message.authCoreId.length !== 0) {
            writer.uint32(42).bytes(message.authCoreId);
        }
        if (message.configCoreId.length !== 0) {
            writer.uint32(50).bytes(message.configCoreId);
        }
        if (message.dataCoreId.length !== 0) {
            writer.uint32(58).bytes(message.dataCoreId);
        }
        if (message.blobCoreId.length !== 0) {
            writer.uint32(66).bytes(message.blobCoreId);
        }
        if (message.blobIndexCoreId.length !== 0) {
            writer.uint32(74).bytes(message.blobIndexCoreId);
        }
        if (message.coreSignatures !== undefined) {
            CoreOwnership_1_CoreSignatures.encode(message.coreSignatures, writer.uint32(82).fork()).ldelim();
        }
        if (message.identitySignature.length !== 0) {
            writer.uint32(90).bytes(message.identitySignature);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseCoreOwnership_1();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.common = Common_1.decode(reader, reader.uint32());
                    continue;
                case 5:
                    if (tag !== 42) {
                        break;
                    }
                    message.authCoreId = reader.bytes();
                    continue;
                case 6:
                    if (tag !== 50) {
                        break;
                    }
                    message.configCoreId = reader.bytes();
                    continue;
                case 7:
                    if (tag !== 58) {
                        break;
                    }
                    message.dataCoreId = reader.bytes();
                    continue;
                case 8:
                    if (tag !== 66) {
                        break;
                    }
                    message.blobCoreId = reader.bytes();
                    continue;
                case 9:
                    if (tag !== 74) {
                        break;
                    }
                    message.blobIndexCoreId = reader.bytes();
                    continue;
                case 10:
                    if (tag !== 82) {
                        break;
                    }
                    message.coreSignatures = CoreOwnership_1_CoreSignatures.decode(reader, reader.uint32());
                    continue;
                case 11:
                    if (tag !== 90) {
                        break;
                    }
                    message.identitySignature = reader.bytes();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            common: isSet(object.common) ? Common_1.fromJSON(object.common) : undefined,
            authCoreId: isSet(object.authCoreId) ? Buffer.from(bytesFromBase64(object.authCoreId)) : Buffer.alloc(0),
            configCoreId: isSet(object.configCoreId) ? Buffer.from(bytesFromBase64(object.configCoreId)) : Buffer.alloc(0),
            dataCoreId: isSet(object.dataCoreId) ? Buffer.from(bytesFromBase64(object.dataCoreId)) : Buffer.alloc(0),
            blobCoreId: isSet(object.blobCoreId) ? Buffer.from(bytesFromBase64(object.blobCoreId)) : Buffer.alloc(0),
            blobIndexCoreId: isSet(object.blobIndexCoreId)
                ? Buffer.from(bytesFromBase64(object.blobIndexCoreId))
                : Buffer.alloc(0),
            coreSignatures: isSet(object.coreSignatures)
                ? CoreOwnership_1_CoreSignatures.fromJSON(object.coreSignatures)
                : undefined,
            identitySignature: isSet(object.identitySignature)
                ? Buffer.from(bytesFromBase64(object.identitySignature))
                : Buffer.alloc(0),
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.common !== undefined) {
            obj.common = Common_1.toJSON(message.common);
        }
        if (message.authCoreId.length !== 0) {
            obj.authCoreId = base64FromBytes(message.authCoreId);
        }
        if (message.configCoreId.length !== 0) {
            obj.configCoreId = base64FromBytes(message.configCoreId);
        }
        if (message.dataCoreId.length !== 0) {
            obj.dataCoreId = base64FromBytes(message.dataCoreId);
        }
        if (message.blobCoreId.length !== 0) {
            obj.blobCoreId = base64FromBytes(message.blobCoreId);
        }
        if (message.blobIndexCoreId.length !== 0) {
            obj.blobIndexCoreId = base64FromBytes(message.blobIndexCoreId);
        }
        if (message.coreSignatures !== undefined) {
            obj.coreSignatures = CoreOwnership_1_CoreSignatures.toJSON(message.coreSignatures);
        }
        if (message.identitySignature.length !== 0) {
            obj.identitySignature = base64FromBytes(message.identitySignature);
        }
        return obj;
    },
    create(base) {
        return CoreOwnership_1.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c, _d, _e, _f;
        const message = createBaseCoreOwnership_1();
        message.common = (object.common !== undefined && object.common !== null)
            ? Common_1.fromPartial(object.common)
            : undefined;
        message.authCoreId = (_a = object.authCoreId) !== null && _a !== void 0 ? _a : Buffer.alloc(0);
        message.configCoreId = (_b = object.configCoreId) !== null && _b !== void 0 ? _b : Buffer.alloc(0);
        message.dataCoreId = (_c = object.dataCoreId) !== null && _c !== void 0 ? _c : Buffer.alloc(0);
        message.blobCoreId = (_d = object.blobCoreId) !== null && _d !== void 0 ? _d : Buffer.alloc(0);
        message.blobIndexCoreId = (_e = object.blobIndexCoreId) !== null && _e !== void 0 ? _e : Buffer.alloc(0);
        message.coreSignatures = (object.coreSignatures !== undefined && object.coreSignatures !== null)
            ? CoreOwnership_1_CoreSignatures.fromPartial(object.coreSignatures)
            : undefined;
        message.identitySignature = (_f = object.identitySignature) !== null && _f !== void 0 ? _f : Buffer.alloc(0);
        return message;
    },
};
function createBaseCoreOwnership_1_CoreSignatures() {
    return {
        auth: Buffer.alloc(0),
        config: Buffer.alloc(0),
        data: Buffer.alloc(0),
        blob: Buffer.alloc(0),
        blobIndex: Buffer.alloc(0),
    };
}
export const CoreOwnership_1_CoreSignatures = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.auth.length !== 0) {
            writer.uint32(10).bytes(message.auth);
        }
        if (message.config.length !== 0) {
            writer.uint32(18).bytes(message.config);
        }
        if (message.data.length !== 0) {
            writer.uint32(26).bytes(message.data);
        }
        if (message.blob.length !== 0) {
            writer.uint32(34).bytes(message.blob);
        }
        if (message.blobIndex.length !== 0) {
            writer.uint32(42).bytes(message.blobIndex);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseCoreOwnership_1_CoreSignatures();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.auth = reader.bytes();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.config = reader.bytes();
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.data = reader.bytes();
                    continue;
                case 4:
                    if (tag !== 34) {
                        break;
                    }
                    message.blob = reader.bytes();
                    continue;
                case 5:
                    if (tag !== 42) {
                        break;
                    }
                    message.blobIndex = reader.bytes();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            auth: isSet(object.auth) ? Buffer.from(bytesFromBase64(object.auth)) : Buffer.alloc(0),
            config: isSet(object.config) ? Buffer.from(bytesFromBase64(object.config)) : Buffer.alloc(0),
            data: isSet(object.data) ? Buffer.from(bytesFromBase64(object.data)) : Buffer.alloc(0),
            blob: isSet(object.blob) ? Buffer.from(bytesFromBase64(object.blob)) : Buffer.alloc(0),
            blobIndex: isSet(object.blobIndex) ? Buffer.from(bytesFromBase64(object.blobIndex)) : Buffer.alloc(0),
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.auth.length !== 0) {
            obj.auth = base64FromBytes(message.auth);
        }
        if (message.config.length !== 0) {
            obj.config = base64FromBytes(message.config);
        }
        if (message.data.length !== 0) {
            obj.data = base64FromBytes(message.data);
        }
        if (message.blob.length !== 0) {
            obj.blob = base64FromBytes(message.blob);
        }
        if (message.blobIndex.length !== 0) {
            obj.blobIndex = base64FromBytes(message.blobIndex);
        }
        return obj;
    },
    create(base) {
        return CoreOwnership_1_CoreSignatures.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c, _d, _e;
        const message = createBaseCoreOwnership_1_CoreSignatures();
        message.auth = (_a = object.auth) !== null && _a !== void 0 ? _a : Buffer.alloc(0);
        message.config = (_b = object.config) !== null && _b !== void 0 ? _b : Buffer.alloc(0);
        message.data = (_c = object.data) !== null && _c !== void 0 ? _c : Buffer.alloc(0);
        message.blob = (_d = object.blob) !== null && _d !== void 0 ? _d : Buffer.alloc(0);
        message.blobIndex = (_e = object.blobIndex) !== null && _e !== void 0 ? _e : Buffer.alloc(0);
        return message;
    },
};
const tsProtoGlobalThis = (() => {
    if (typeof globalThis !== "undefined") {
        return globalThis;
    }
    if (typeof self !== "undefined") {
        return self;
    }
    if (typeof window !== "undefined") {
        return window;
    }
    if (typeof global !== "undefined") {
        return global;
    }
    throw "Unable to locate global object";
})();
function bytesFromBase64(b64) {
    if (tsProtoGlobalThis.Buffer) {
        return Uint8Array.from(tsProtoGlobalThis.Buffer.from(b64, "base64"));
    }
    else {
        const bin = tsProtoGlobalThis.atob(b64);
        const arr = new Uint8Array(bin.length);
        for (let i = 0; i < bin.length; ++i) {
            arr[i] = bin.charCodeAt(i);
        }
        return arr;
    }
}
function base64FromBytes(arr) {
    if (tsProtoGlobalThis.Buffer) {
        return tsProtoGlobalThis.Buffer.from(arr).toString("base64");
    }
    else {
        const bin = [];
        arr.forEach((byte) => {
            bin.push(String.fromCharCode(byte));
        });
        return tsProtoGlobalThis.btoa(bin.join(""));
    }
}
function isSet(value) {
    return value !== null && value !== undefined;
}
