/* eslint-disable */
import _m0 from "protobufjs/minimal.js";
import { Timestamp } from "../google/protobuf/timestamp.js";
import { VersionId_1 } from "../versionId/v1.js";
export const protobufPackage = "mapeo";
function createBaseCommon_1() {
    return {
        docId: Buffer.alloc(0),
        links: [],
        createdAt: undefined,
        updatedAt: undefined,
        originalVersionId: undefined,
        deleted: false,
    };
}
export const Common_1 = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.docId.length !== 0) {
            writer.uint32(10).bytes(message.docId);
        }
        for (const v of message.links) {
            VersionId_1.encode(v, writer.uint32(18).fork()).ldelim();
        }
        if (message.createdAt !== undefined) {
            Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(26).fork()).ldelim();
        }
        if (message.updatedAt !== undefined) {
            Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(34).fork()).ldelim();
        }
        if (message.originalVersionId !== undefined) {
            VersionId_1.encode(message.originalVersionId, writer.uint32(42).fork()).ldelim();
        }
        if (message.deleted === true) {
            writer.uint32(48).bool(message.deleted);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseCommon_1();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.docId = reader.bytes();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.links.push(VersionId_1.decode(reader, reader.uint32()));
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
                    continue;
                case 4:
                    if (tag !== 34) {
                        break;
                    }
                    message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
                    continue;
                case 5:
                    if (tag !== 42) {
                        break;
                    }
                    message.originalVersionId = VersionId_1.decode(reader, reader.uint32());
                    continue;
                case 6:
                    if (tag !== 48) {
                        break;
                    }
                    message.deleted = reader.bool();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            docId: isSet(object.docId) ? Buffer.from(bytesFromBase64(object.docId)) : Buffer.alloc(0),
            links: Array.isArray(object === null || object === void 0 ? void 0 : object.links) ? object.links.map((e) => VersionId_1.fromJSON(e)) : [],
            createdAt: isSet(object.createdAt) ? String(object.createdAt) : undefined,
            updatedAt: isSet(object.updatedAt) ? String(object.updatedAt) : undefined,
            originalVersionId: isSet(object.originalVersionId) ? VersionId_1.fromJSON(object.originalVersionId) : undefined,
            deleted: isSet(object.deleted) ? Boolean(object.deleted) : false,
        };
    },
    toJSON(message) {
        var _a;
        const obj = {};
        if (message.docId.length !== 0) {
            obj.docId = base64FromBytes(message.docId);
        }
        if ((_a = message.links) === null || _a === void 0 ? void 0 : _a.length) {
            obj.links = message.links.map((e) => VersionId_1.toJSON(e));
        }
        if (message.createdAt !== undefined) {
            obj.createdAt = message.createdAt;
        }
        if (message.updatedAt !== undefined) {
            obj.updatedAt = message.updatedAt;
        }
        if (message.originalVersionId !== undefined) {
            obj.originalVersionId = VersionId_1.toJSON(message.originalVersionId);
        }
        if (message.deleted === true) {
            obj.deleted = message.deleted;
        }
        return obj;
    },
    create(base) {
        return Common_1.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c, _d, _e;
        const message = createBaseCommon_1();
        message.docId = (_a = object.docId) !== null && _a !== void 0 ? _a : Buffer.alloc(0);
        message.links = ((_b = object.links) === null || _b === void 0 ? void 0 : _b.map((e) => VersionId_1.fromPartial(e))) || [];
        message.createdAt = (_c = object.createdAt) !== null && _c !== void 0 ? _c : undefined;
        message.updatedAt = (_d = object.updatedAt) !== null && _d !== void 0 ? _d : undefined;
        message.originalVersionId = (object.originalVersionId !== undefined && object.originalVersionId !== null)
            ? VersionId_1.fromPartial(object.originalVersionId)
            : undefined;
        message.deleted = (_e = object.deleted) !== null && _e !== void 0 ? _e : false;
        return message;
    },
};
const tsProtoGlobalThis = (() => {
    if (typeof globalThis !== "undefined") {
        return globalThis;
    }
    if (typeof self !== "undefined") {
        return self;
    }
    if (typeof window !== "undefined") {
        return window;
    }
    if (typeof global !== "undefined") {
        return global;
    }
    throw "Unable to locate global object";
})();
function bytesFromBase64(b64) {
    if (tsProtoGlobalThis.Buffer) {
        return Uint8Array.from(tsProtoGlobalThis.Buffer.from(b64, "base64"));
    }
    else {
        const bin = tsProtoGlobalThis.atob(b64);
        const arr = new Uint8Array(bin.length);
        for (let i = 0; i < bin.length; ++i) {
            arr[i] = bin.charCodeAt(i);
        }
        return arr;
    }
}
function base64FromBytes(arr) {
    if (tsProtoGlobalThis.Buffer) {
        return tsProtoGlobalThis.Buffer.from(arr).toString("base64");
    }
    else {
        const bin = [];
        arr.forEach((byte) => {
            bin.push(String.fromCharCode(byte));
        });
        return tsProtoGlobalThis.btoa(bin.join(""));
    }
}
function toTimestamp(dateStr) {
    const date = new Date(dateStr);
    const seconds = date.getTime() / 1000;
    const nanos = (date.getTime() % 1000) * 1000000;
    return { seconds, nanos };
}
function fromTimestamp(t) {
    let millis = (t.seconds || 0) * 1000;
    millis += (t.nanos || 0) / 1000000;
    return new Date(millis).toISOString();
}
function isSet(value) {
    return value !== null && value !== undefined;
}
