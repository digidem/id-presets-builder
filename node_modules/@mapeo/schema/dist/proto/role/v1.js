/* eslint-disable */
import _m0 from "protobufjs/minimal.js";
import { Common_1 } from "../common/v1.js";
export const protobufPackage = "mapeo";
function createBaseRole_1() {
    return { common: undefined, roleId: Buffer.alloc(0), fromIndex: 0 };
}
export const Role_1 = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.common !== undefined) {
            Common_1.encode(message.common, writer.uint32(10).fork()).ldelim();
        }
        if (message.roleId.length !== 0) {
            writer.uint32(42).bytes(message.roleId);
        }
        if (message.fromIndex !== 0) {
            writer.uint32(48).uint32(message.fromIndex);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseRole_1();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.common = Common_1.decode(reader, reader.uint32());
                    continue;
                case 5:
                    if (tag !== 42) {
                        break;
                    }
                    message.roleId = reader.bytes();
                    continue;
                case 6:
                    if (tag !== 48) {
                        break;
                    }
                    message.fromIndex = reader.uint32();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            common: isSet(object.common) ? Common_1.fromJSON(object.common) : undefined,
            roleId: isSet(object.roleId) ? Buffer.from(bytesFromBase64(object.roleId)) : Buffer.alloc(0),
            fromIndex: isSet(object.fromIndex) ? Number(object.fromIndex) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.common !== undefined) {
            obj.common = Common_1.toJSON(message.common);
        }
        if (message.roleId.length !== 0) {
            obj.roleId = base64FromBytes(message.roleId);
        }
        if (message.fromIndex !== 0) {
            obj.fromIndex = Math.round(message.fromIndex);
        }
        return obj;
    },
    create(base) {
        return Role_1.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b;
        const message = createBaseRole_1();
        message.common = (object.common !== undefined && object.common !== null)
            ? Common_1.fromPartial(object.common)
            : undefined;
        message.roleId = (_a = object.roleId) !== null && _a !== void 0 ? _a : Buffer.alloc(0);
        message.fromIndex = (_b = object.fromIndex) !== null && _b !== void 0 ? _b : 0;
        return message;
    },
};
const tsProtoGlobalThis = (() => {
    if (typeof globalThis !== "undefined") {
        return globalThis;
    }
    if (typeof self !== "undefined") {
        return self;
    }
    if (typeof window !== "undefined") {
        return window;
    }
    if (typeof global !== "undefined") {
        return global;
    }
    throw "Unable to locate global object";
})();
function bytesFromBase64(b64) {
    if (tsProtoGlobalThis.Buffer) {
        return Uint8Array.from(tsProtoGlobalThis.Buffer.from(b64, "base64"));
    }
    else {
        const bin = tsProtoGlobalThis.atob(b64);
        const arr = new Uint8Array(bin.length);
        for (let i = 0; i < bin.length; ++i) {
            arr[i] = bin.charCodeAt(i);
        }
        return arr;
    }
}
function base64FromBytes(arr) {
    if (tsProtoGlobalThis.Buffer) {
        return tsProtoGlobalThis.Buffer.from(arr).toString("base64");
    }
    else {
        const bin = [];
        arr.forEach((byte) => {
            bin.push(String.fromCharCode(byte));
        });
        return tsProtoGlobalThis.btoa(bin.join(""));
    }
}
function isSet(value) {
    return value !== null && value !== undefined;
}
