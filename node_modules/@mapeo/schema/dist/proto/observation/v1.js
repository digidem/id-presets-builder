/* eslint-disable */
import _m0 from "protobufjs/minimal.js";
import { Common_1 } from "../common/v1.js";
import { Timestamp } from "../google/protobuf/timestamp.js";
import { TagValue_1 } from "../tags/v1.js";
import { VersionId_1 } from "../versionId/v1.js";
export const protobufPackage = "mapeo";
/** ATTACHMENT */
export const Observation_1_AttachmentType = {
    attachment_type_unspecified: "attachment_type_unspecified",
    photo: "photo",
    video: "video",
    audio: "audio",
    UNRECOGNIZED: "UNRECOGNIZED",
};
export function observation_1_AttachmentTypeFromJSON(object) {
    switch (object) {
        case 0:
        case "attachment_type_unspecified":
            return Observation_1_AttachmentType.attachment_type_unspecified;
        case 1:
        case "photo":
            return Observation_1_AttachmentType.photo;
        case 2:
        case "video":
            return Observation_1_AttachmentType.video;
        case 3:
        case "audio":
            return Observation_1_AttachmentType.audio;
        case -1:
        case "UNRECOGNIZED":
        default:
            return Observation_1_AttachmentType.UNRECOGNIZED;
    }
}
export function observation_1_AttachmentTypeToJSON(object) {
    switch (object) {
        case Observation_1_AttachmentType.attachment_type_unspecified:
            return "attachment_type_unspecified";
        case Observation_1_AttachmentType.photo:
            return "photo";
        case Observation_1_AttachmentType.video:
            return "video";
        case Observation_1_AttachmentType.audio:
            return "audio";
        case Observation_1_AttachmentType.UNRECOGNIZED:
        default:
            return "UNRECOGNIZED";
    }
}
export function observation_1_AttachmentTypeToNumber(object) {
    switch (object) {
        case Observation_1_AttachmentType.attachment_type_unspecified:
            return 0;
        case Observation_1_AttachmentType.photo:
            return 1;
        case Observation_1_AttachmentType.video:
            return 2;
        case Observation_1_AttachmentType.audio:
            return 3;
        case Observation_1_AttachmentType.UNRECOGNIZED:
        default:
            return -1;
    }
}
function createBaseObservation_1() {
    return { common: undefined, lat: 0, lon: 0, attachments: [], tags: {}, metadata: undefined, presetRef: undefined };
}
export const Observation_1 = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.common !== undefined) {
            Common_1.encode(message.common, writer.uint32(10).fork()).ldelim();
        }
        if (message.lat !== 0) {
            writer.uint32(41).double(message.lat);
        }
        if (message.lon !== 0) {
            writer.uint32(49).double(message.lon);
        }
        for (const v of message.attachments) {
            Observation_1_Attachment.encode(v, writer.uint32(58).fork()).ldelim();
        }
        Object.entries(message.tags).forEach(([key, value]) => {
            Observation_1_TagsEntry.encode({ key: key, value }, writer.uint32(66).fork()).ldelim();
        });
        if (message.metadata !== undefined) {
            Observation_1_Metadata.encode(message.metadata, writer.uint32(74).fork()).ldelim();
        }
        if (message.presetRef !== undefined) {
            Observation_1_PresetRef.encode(message.presetRef, writer.uint32(82).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseObservation_1();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.common = Common_1.decode(reader, reader.uint32());
                    continue;
                case 5:
                    if (tag !== 41) {
                        break;
                    }
                    message.lat = reader.double();
                    continue;
                case 6:
                    if (tag !== 49) {
                        break;
                    }
                    message.lon = reader.double();
                    continue;
                case 7:
                    if (tag !== 58) {
                        break;
                    }
                    message.attachments.push(Observation_1_Attachment.decode(reader, reader.uint32()));
                    continue;
                case 8:
                    if (tag !== 66) {
                        break;
                    }
                    const entry8 = Observation_1_TagsEntry.decode(reader, reader.uint32());
                    if (entry8.value !== undefined) {
                        message.tags[entry8.key] = entry8.value;
                    }
                    continue;
                case 9:
                    if (tag !== 74) {
                        break;
                    }
                    message.metadata = Observation_1_Metadata.decode(reader, reader.uint32());
                    continue;
                case 10:
                    if (tag !== 82) {
                        break;
                    }
                    message.presetRef = Observation_1_PresetRef.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            common: isSet(object.common) ? Common_1.fromJSON(object.common) : undefined,
            lat: isSet(object.lat) ? Number(object.lat) : 0,
            lon: isSet(object.lon) ? Number(object.lon) : 0,
            attachments: Array.isArray(object === null || object === void 0 ? void 0 : object.attachments)
                ? object.attachments.map((e) => Observation_1_Attachment.fromJSON(e))
                : [],
            tags: isObject(object.tags)
                ? Object.entries(object.tags).reduce((acc, [key, value]) => {
                    acc[key] = TagValue_1.fromJSON(value);
                    return acc;
                }, {})
                : {},
            metadata: isSet(object.metadata) ? Observation_1_Metadata.fromJSON(object.metadata) : undefined,
            presetRef: isSet(object.presetRef) ? Observation_1_PresetRef.fromJSON(object.presetRef) : undefined,
        };
    },
    toJSON(message) {
        var _a;
        const obj = {};
        if (message.common !== undefined) {
            obj.common = Common_1.toJSON(message.common);
        }
        if (message.lat !== 0) {
            obj.lat = message.lat;
        }
        if (message.lon !== 0) {
            obj.lon = message.lon;
        }
        if ((_a = message.attachments) === null || _a === void 0 ? void 0 : _a.length) {
            obj.attachments = message.attachments.map((e) => Observation_1_Attachment.toJSON(e));
        }
        if (message.tags) {
            const entries = Object.entries(message.tags);
            if (entries.length > 0) {
                obj.tags = {};
                entries.forEach(([k, v]) => {
                    obj.tags[k] = TagValue_1.toJSON(v);
                });
            }
        }
        if (message.metadata !== undefined) {
            obj.metadata = Observation_1_Metadata.toJSON(message.metadata);
        }
        if (message.presetRef !== undefined) {
            obj.presetRef = Observation_1_PresetRef.toJSON(message.presetRef);
        }
        return obj;
    },
    create(base) {
        return Observation_1.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c, _d;
        const message = createBaseObservation_1();
        message.common = (object.common !== undefined && object.common !== null)
            ? Common_1.fromPartial(object.common)
            : undefined;
        message.lat = (_a = object.lat) !== null && _a !== void 0 ? _a : 0;
        message.lon = (_b = object.lon) !== null && _b !== void 0 ? _b : 0;
        message.attachments = ((_c = object.attachments) === null || _c === void 0 ? void 0 : _c.map((e) => Observation_1_Attachment.fromPartial(e))) || [];
        message.tags = Object.entries((_d = object.tags) !== null && _d !== void 0 ? _d : {}).reduce((acc, [key, value]) => {
            if (value !== undefined) {
                acc[key] = TagValue_1.fromPartial(value);
            }
            return acc;
        }, {});
        message.metadata = (object.metadata !== undefined && object.metadata !== null)
            ? Observation_1_Metadata.fromPartial(object.metadata)
            : undefined;
        message.presetRef = (object.presetRef !== undefined && object.presetRef !== null)
            ? Observation_1_PresetRef.fromPartial(object.presetRef)
            : undefined;
        return message;
    },
};
function createBaseObservation_1_Attachment() {
    return {
        driveDiscoveryId: Buffer.alloc(0),
        name: "",
        type: Observation_1_AttachmentType.attachment_type_unspecified,
        hash: Buffer.alloc(0),
    };
}
export const Observation_1_Attachment = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.driveDiscoveryId.length !== 0) {
            writer.uint32(10).bytes(message.driveDiscoveryId);
        }
        if (message.name !== "") {
            writer.uint32(18).string(message.name);
        }
        if (message.type !== Observation_1_AttachmentType.attachment_type_unspecified) {
            writer.uint32(24).int32(observation_1_AttachmentTypeToNumber(message.type));
        }
        if (message.hash.length !== 0) {
            writer.uint32(34).bytes(message.hash);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseObservation_1_Attachment();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.driveDiscoveryId = reader.bytes();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.name = reader.string();
                    continue;
                case 3:
                    if (tag !== 24) {
                        break;
                    }
                    message.type = observation_1_AttachmentTypeFromJSON(reader.int32());
                    continue;
                case 4:
                    if (tag !== 34) {
                        break;
                    }
                    message.hash = reader.bytes();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            driveDiscoveryId: isSet(object.driveDiscoveryId)
                ? Buffer.from(bytesFromBase64(object.driveDiscoveryId))
                : Buffer.alloc(0),
            name: isSet(object.name) ? String(object.name) : "",
            type: isSet(object.type)
                ? observation_1_AttachmentTypeFromJSON(object.type)
                : Observation_1_AttachmentType.attachment_type_unspecified,
            hash: isSet(object.hash) ? Buffer.from(bytesFromBase64(object.hash)) : Buffer.alloc(0),
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.driveDiscoveryId.length !== 0) {
            obj.driveDiscoveryId = base64FromBytes(message.driveDiscoveryId);
        }
        if (message.name !== "") {
            obj.name = message.name;
        }
        if (message.type !== Observation_1_AttachmentType.attachment_type_unspecified) {
            obj.type = observation_1_AttachmentTypeToJSON(message.type);
        }
        if (message.hash.length !== 0) {
            obj.hash = base64FromBytes(message.hash);
        }
        return obj;
    },
    create(base) {
        return Observation_1_Attachment.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c, _d;
        const message = createBaseObservation_1_Attachment();
        message.driveDiscoveryId = (_a = object.driveDiscoveryId) !== null && _a !== void 0 ? _a : Buffer.alloc(0);
        message.name = (_b = object.name) !== null && _b !== void 0 ? _b : "";
        message.type = (_c = object.type) !== null && _c !== void 0 ? _c : Observation_1_AttachmentType.attachment_type_unspecified;
        message.hash = (_d = object.hash) !== null && _d !== void 0 ? _d : Buffer.alloc(0);
        return message;
    },
};
function createBaseObservation_1_TagsEntry() {
    return { key: "", value: undefined };
}
export const Observation_1_TagsEntry = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.key !== "") {
            writer.uint32(10).string(message.key);
        }
        if (message.value !== undefined) {
            TagValue_1.encode(message.value, writer.uint32(18).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseObservation_1_TagsEntry();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.key = reader.string();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.value = TagValue_1.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            key: isSet(object.key) ? String(object.key) : "",
            value: isSet(object.value) ? TagValue_1.fromJSON(object.value) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.key !== "") {
            obj.key = message.key;
        }
        if (message.value !== undefined) {
            obj.value = TagValue_1.toJSON(message.value);
        }
        return obj;
    },
    create(base) {
        return Observation_1_TagsEntry.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a;
        const message = createBaseObservation_1_TagsEntry();
        message.key = (_a = object.key) !== null && _a !== void 0 ? _a : "";
        message.value = (object.value !== undefined && object.value !== null)
            ? TagValue_1.fromPartial(object.value)
            : undefined;
        return message;
    },
};
function createBaseObservation_1_Metadata() {
    return { manualLocation: false, position: undefined, lastSavedPosition: undefined, positionProvider: undefined };
}
export const Observation_1_Metadata = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.manualLocation === true) {
            writer.uint32(8).bool(message.manualLocation);
        }
        if (message.position !== undefined) {
            Observation_1_Metadata_Position.encode(message.position, writer.uint32(26).fork()).ldelim();
        }
        if (message.lastSavedPosition !== undefined) {
            Observation_1_Metadata_Position.encode(message.lastSavedPosition, writer.uint32(34).fork()).ldelim();
        }
        if (message.positionProvider !== undefined) {
            Observation_1_Metadata_PositionProvider.encode(message.positionProvider, writer.uint32(42).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseObservation_1_Metadata();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 8) {
                        break;
                    }
                    message.manualLocation = reader.bool();
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.position = Observation_1_Metadata_Position.decode(reader, reader.uint32());
                    continue;
                case 4:
                    if (tag !== 34) {
                        break;
                    }
                    message.lastSavedPosition = Observation_1_Metadata_Position.decode(reader, reader.uint32());
                    continue;
                case 5:
                    if (tag !== 42) {
                        break;
                    }
                    message.positionProvider = Observation_1_Metadata_PositionProvider.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            manualLocation: isSet(object.manualLocation) ? Boolean(object.manualLocation) : false,
            position: isSet(object.position) ? Observation_1_Metadata_Position.fromJSON(object.position) : undefined,
            lastSavedPosition: isSet(object.lastSavedPosition)
                ? Observation_1_Metadata_Position.fromJSON(object.lastSavedPosition)
                : undefined,
            positionProvider: isSet(object.positionProvider)
                ? Observation_1_Metadata_PositionProvider.fromJSON(object.positionProvider)
                : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.manualLocation === true) {
            obj.manualLocation = message.manualLocation;
        }
        if (message.position !== undefined) {
            obj.position = Observation_1_Metadata_Position.toJSON(message.position);
        }
        if (message.lastSavedPosition !== undefined) {
            obj.lastSavedPosition = Observation_1_Metadata_Position.toJSON(message.lastSavedPosition);
        }
        if (message.positionProvider !== undefined) {
            obj.positionProvider = Observation_1_Metadata_PositionProvider.toJSON(message.positionProvider);
        }
        return obj;
    },
    create(base) {
        return Observation_1_Metadata.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a;
        const message = createBaseObservation_1_Metadata();
        message.manualLocation = (_a = object.manualLocation) !== null && _a !== void 0 ? _a : false;
        message.position = (object.position !== undefined && object.position !== null)
            ? Observation_1_Metadata_Position.fromPartial(object.position)
            : undefined;
        message.lastSavedPosition = (object.lastSavedPosition !== undefined && object.lastSavedPosition !== null)
            ? Observation_1_Metadata_Position.fromPartial(object.lastSavedPosition)
            : undefined;
        message.positionProvider = (object.positionProvider !== undefined && object.positionProvider !== null)
            ? Observation_1_Metadata_PositionProvider.fromPartial(object.positionProvider)
            : undefined;
        return message;
    },
};
function createBaseObservation_1_Metadata_Position() {
    return { timestamp: undefined, mocked: false, coords: undefined };
}
export const Observation_1_Metadata_Position = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.timestamp !== undefined) {
            Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(10).fork()).ldelim();
        }
        if (message.mocked === true) {
            writer.uint32(16).bool(message.mocked);
        }
        if (message.coords !== undefined) {
            Observation_1_Metadata_Position_Coords.encode(message.coords, writer.uint32(26).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseObservation_1_Metadata_Position();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
                    continue;
                case 2:
                    if (tag !== 16) {
                        break;
                    }
                    message.mocked = reader.bool();
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.coords = Observation_1_Metadata_Position_Coords.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            timestamp: isSet(object.timestamp) ? String(object.timestamp) : undefined,
            mocked: isSet(object.mocked) ? Boolean(object.mocked) : false,
            coords: isSet(object.coords) ? Observation_1_Metadata_Position_Coords.fromJSON(object.coords) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.timestamp !== undefined) {
            obj.timestamp = message.timestamp;
        }
        if (message.mocked === true) {
            obj.mocked = message.mocked;
        }
        if (message.coords !== undefined) {
            obj.coords = Observation_1_Metadata_Position_Coords.toJSON(message.coords);
        }
        return obj;
    },
    create(base) {
        return Observation_1_Metadata_Position.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b;
        const message = createBaseObservation_1_Metadata_Position();
        message.timestamp = (_a = object.timestamp) !== null && _a !== void 0 ? _a : undefined;
        message.mocked = (_b = object.mocked) !== null && _b !== void 0 ? _b : false;
        message.coords = (object.coords !== undefined && object.coords !== null)
            ? Observation_1_Metadata_Position_Coords.fromPartial(object.coords)
            : undefined;
        return message;
    },
};
function createBaseObservation_1_Metadata_Position_Coords() {
    return { latitude: 0, longitude: 0, altitude: 0, heading: 0, speed: 0, accuracy: 0 };
}
export const Observation_1_Metadata_Position_Coords = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.latitude !== 0) {
            writer.uint32(9).double(message.latitude);
        }
        if (message.longitude !== 0) {
            writer.uint32(17).double(message.longitude);
        }
        if (message.altitude !== 0) {
            writer.uint32(25).double(message.altitude);
        }
        if (message.heading !== 0) {
            writer.uint32(33).double(message.heading);
        }
        if (message.speed !== 0) {
            writer.uint32(41).double(message.speed);
        }
        if (message.accuracy !== 0) {
            writer.uint32(49).double(message.accuracy);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseObservation_1_Metadata_Position_Coords();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 9) {
                        break;
                    }
                    message.latitude = reader.double();
                    continue;
                case 2:
                    if (tag !== 17) {
                        break;
                    }
                    message.longitude = reader.double();
                    continue;
                case 3:
                    if (tag !== 25) {
                        break;
                    }
                    message.altitude = reader.double();
                    continue;
                case 4:
                    if (tag !== 33) {
                        break;
                    }
                    message.heading = reader.double();
                    continue;
                case 5:
                    if (tag !== 41) {
                        break;
                    }
                    message.speed = reader.double();
                    continue;
                case 6:
                    if (tag !== 49) {
                        break;
                    }
                    message.accuracy = reader.double();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            latitude: isSet(object.latitude) ? Number(object.latitude) : 0,
            longitude: isSet(object.longitude) ? Number(object.longitude) : 0,
            altitude: isSet(object.altitude) ? Number(object.altitude) : 0,
            heading: isSet(object.heading) ? Number(object.heading) : 0,
            speed: isSet(object.speed) ? Number(object.speed) : 0,
            accuracy: isSet(object.accuracy) ? Number(object.accuracy) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.latitude !== 0) {
            obj.latitude = message.latitude;
        }
        if (message.longitude !== 0) {
            obj.longitude = message.longitude;
        }
        if (message.altitude !== 0) {
            obj.altitude = message.altitude;
        }
        if (message.heading !== 0) {
            obj.heading = message.heading;
        }
        if (message.speed !== 0) {
            obj.speed = message.speed;
        }
        if (message.accuracy !== 0) {
            obj.accuracy = message.accuracy;
        }
        return obj;
    },
    create(base) {
        return Observation_1_Metadata_Position_Coords.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c, _d, _e, _f;
        const message = createBaseObservation_1_Metadata_Position_Coords();
        message.latitude = (_a = object.latitude) !== null && _a !== void 0 ? _a : 0;
        message.longitude = (_b = object.longitude) !== null && _b !== void 0 ? _b : 0;
        message.altitude = (_c = object.altitude) !== null && _c !== void 0 ? _c : 0;
        message.heading = (_d = object.heading) !== null && _d !== void 0 ? _d : 0;
        message.speed = (_e = object.speed) !== null && _e !== void 0 ? _e : 0;
        message.accuracy = (_f = object.accuracy) !== null && _f !== void 0 ? _f : 0;
        return message;
    },
};
function createBaseObservation_1_Metadata_PositionProvider() {
    return { gpsAvailable: false, passiveAvailable: false, locationServicesEnabled: false, networkAvailable: false };
}
export const Observation_1_Metadata_PositionProvider = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.gpsAvailable === true) {
            writer.uint32(8).bool(message.gpsAvailable);
        }
        if (message.passiveAvailable === true) {
            writer.uint32(16).bool(message.passiveAvailable);
        }
        if (message.locationServicesEnabled === true) {
            writer.uint32(24).bool(message.locationServicesEnabled);
        }
        if (message.networkAvailable === true) {
            writer.uint32(32).bool(message.networkAvailable);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseObservation_1_Metadata_PositionProvider();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 8) {
                        break;
                    }
                    message.gpsAvailable = reader.bool();
                    continue;
                case 2:
                    if (tag !== 16) {
                        break;
                    }
                    message.passiveAvailable = reader.bool();
                    continue;
                case 3:
                    if (tag !== 24) {
                        break;
                    }
                    message.locationServicesEnabled = reader.bool();
                    continue;
                case 4:
                    if (tag !== 32) {
                        break;
                    }
                    message.networkAvailable = reader.bool();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            gpsAvailable: isSet(object.gpsAvailable) ? Boolean(object.gpsAvailable) : false,
            passiveAvailable: isSet(object.passiveAvailable) ? Boolean(object.passiveAvailable) : false,
            locationServicesEnabled: isSet(object.locationServicesEnabled) ? Boolean(object.locationServicesEnabled) : false,
            networkAvailable: isSet(object.networkAvailable) ? Boolean(object.networkAvailable) : false,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.gpsAvailable === true) {
            obj.gpsAvailable = message.gpsAvailable;
        }
        if (message.passiveAvailable === true) {
            obj.passiveAvailable = message.passiveAvailable;
        }
        if (message.locationServicesEnabled === true) {
            obj.locationServicesEnabled = message.locationServicesEnabled;
        }
        if (message.networkAvailable === true) {
            obj.networkAvailable = message.networkAvailable;
        }
        return obj;
    },
    create(base) {
        return Observation_1_Metadata_PositionProvider.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c, _d;
        const message = createBaseObservation_1_Metadata_PositionProvider();
        message.gpsAvailable = (_a = object.gpsAvailable) !== null && _a !== void 0 ? _a : false;
        message.passiveAvailable = (_b = object.passiveAvailable) !== null && _b !== void 0 ? _b : false;
        message.locationServicesEnabled = (_c = object.locationServicesEnabled) !== null && _c !== void 0 ? _c : false;
        message.networkAvailable = (_d = object.networkAvailable) !== null && _d !== void 0 ? _d : false;
        return message;
    },
};
function createBaseObservation_1_PresetRef() {
    return { docId: Buffer.alloc(0), versionId: undefined };
}
export const Observation_1_PresetRef = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.docId.length !== 0) {
            writer.uint32(10).bytes(message.docId);
        }
        if (message.versionId !== undefined) {
            VersionId_1.encode(message.versionId, writer.uint32(18).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseObservation_1_PresetRef();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.docId = reader.bytes();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.versionId = VersionId_1.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            docId: isSet(object.docId) ? Buffer.from(bytesFromBase64(object.docId)) : Buffer.alloc(0),
            versionId: isSet(object.versionId) ? VersionId_1.fromJSON(object.versionId) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.docId.length !== 0) {
            obj.docId = base64FromBytes(message.docId);
        }
        if (message.versionId !== undefined) {
            obj.versionId = VersionId_1.toJSON(message.versionId);
        }
        return obj;
    },
    create(base) {
        return Observation_1_PresetRef.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a;
        const message = createBaseObservation_1_PresetRef();
        message.docId = (_a = object.docId) !== null && _a !== void 0 ? _a : Buffer.alloc(0);
        message.versionId = (object.versionId !== undefined && object.versionId !== null)
            ? VersionId_1.fromPartial(object.versionId)
            : undefined;
        return message;
    },
};
const tsProtoGlobalThis = (() => {
    if (typeof globalThis !== "undefined") {
        return globalThis;
    }
    if (typeof self !== "undefined") {
        return self;
    }
    if (typeof window !== "undefined") {
        return window;
    }
    if (typeof global !== "undefined") {
        return global;
    }
    throw "Unable to locate global object";
})();
function bytesFromBase64(b64) {
    if (tsProtoGlobalThis.Buffer) {
        return Uint8Array.from(tsProtoGlobalThis.Buffer.from(b64, "base64"));
    }
    else {
        const bin = tsProtoGlobalThis.atob(b64);
        const arr = new Uint8Array(bin.length);
        for (let i = 0; i < bin.length; ++i) {
            arr[i] = bin.charCodeAt(i);
        }
        return arr;
    }
}
function base64FromBytes(arr) {
    if (tsProtoGlobalThis.Buffer) {
        return tsProtoGlobalThis.Buffer.from(arr).toString("base64");
    }
    else {
        const bin = [];
        arr.forEach((byte) => {
            bin.push(String.fromCharCode(byte));
        });
        return tsProtoGlobalThis.btoa(bin.join(""));
    }
}
function toTimestamp(dateStr) {
    const date = new Date(dateStr);
    const seconds = date.getTime() / 1000;
    const nanos = (date.getTime() % 1000) * 1000000;
    return { seconds, nanos };
}
function fromTimestamp(t) {
    let millis = (t.seconds || 0) * 1000;
    millis += (t.nanos || 0) / 1000000;
    return new Date(millis).toISOString();
}
function isObject(value) {
    return typeof value === "object" && value !== null;
}
function isSet(value) {
    return value !== null && value !== undefined;
}
