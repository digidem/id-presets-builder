/* eslint-disable */
import _m0 from "protobufjs/minimal.js";
import { Common_1 } from "../common/v1.js";
import { TagValue_1 } from "../tags/v1.js";
import { VersionId_1 } from "../versionId/v1.js";
export const protobufPackage = "mapeo";
export const Preset_1_Geometry = {
    geometry_unspecified: "geometry_unspecified",
    point: "point",
    vertex: "vertex",
    line: "line",
    area: "area",
    relation: "relation",
    UNRECOGNIZED: "UNRECOGNIZED",
};
export function preset_1_GeometryFromJSON(object) {
    switch (object) {
        case 0:
        case "geometry_unspecified":
            return Preset_1_Geometry.geometry_unspecified;
        case 1:
        case "point":
            return Preset_1_Geometry.point;
        case 2:
        case "vertex":
            return Preset_1_Geometry.vertex;
        case 3:
        case "line":
            return Preset_1_Geometry.line;
        case 4:
        case "area":
            return Preset_1_Geometry.area;
        case 5:
        case "relation":
            return Preset_1_Geometry.relation;
        case -1:
        case "UNRECOGNIZED":
        default:
            return Preset_1_Geometry.UNRECOGNIZED;
    }
}
export function preset_1_GeometryToJSON(object) {
    switch (object) {
        case Preset_1_Geometry.geometry_unspecified:
            return "geometry_unspecified";
        case Preset_1_Geometry.point:
            return "point";
        case Preset_1_Geometry.vertex:
            return "vertex";
        case Preset_1_Geometry.line:
            return "line";
        case Preset_1_Geometry.area:
            return "area";
        case Preset_1_Geometry.relation:
            return "relation";
        case Preset_1_Geometry.UNRECOGNIZED:
        default:
            return "UNRECOGNIZED";
    }
}
export function preset_1_GeometryToNumber(object) {
    switch (object) {
        case Preset_1_Geometry.geometry_unspecified:
            return 0;
        case Preset_1_Geometry.point:
            return 1;
        case Preset_1_Geometry.vertex:
            return 2;
        case Preset_1_Geometry.line:
            return 3;
        case Preset_1_Geometry.area:
            return 4;
        case Preset_1_Geometry.relation:
            return 5;
        case Preset_1_Geometry.UNRECOGNIZED:
        default:
            return -1;
    }
}
function createBasePreset_1() {
    return {
        common: undefined,
        name: "",
        geometry: [],
        tags: {},
        addTags: {},
        removeTags: {},
        fieldRefs: [],
        iconRef: undefined,
        terms: [],
        color: "",
    };
}
export const Preset_1 = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.common !== undefined) {
            Common_1.encode(message.common, writer.uint32(10).fork()).ldelim();
        }
        if (message.name !== "") {
            writer.uint32(42).string(message.name);
        }
        writer.uint32(50).fork();
        for (const v of message.geometry) {
            writer.int32(preset_1_GeometryToNumber(v));
        }
        writer.ldelim();
        Object.entries(message.tags).forEach(([key, value]) => {
            Preset_1_TagsEntry.encode({ key: key, value }, writer.uint32(58).fork()).ldelim();
        });
        Object.entries(message.addTags).forEach(([key, value]) => {
            Preset_1_AddTagsEntry.encode({ key: key, value }, writer.uint32(66).fork()).ldelim();
        });
        Object.entries(message.removeTags).forEach(([key, value]) => {
            Preset_1_RemoveTagsEntry.encode({ key: key, value }, writer.uint32(74).fork()).ldelim();
        });
        for (const v of message.fieldRefs) {
            Preset_1_FieldRef.encode(v, writer.uint32(82).fork()).ldelim();
        }
        if (message.iconRef !== undefined) {
            Preset_1_IconRef.encode(message.iconRef, writer.uint32(90).fork()).ldelim();
        }
        for (const v of message.terms) {
            writer.uint32(98).string(v);
        }
        if (message.color !== "") {
            writer.uint32(106).string(message.color);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBasePreset_1();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.common = Common_1.decode(reader, reader.uint32());
                    continue;
                case 5:
                    if (tag !== 42) {
                        break;
                    }
                    message.name = reader.string();
                    continue;
                case 6:
                    if (tag === 48) {
                        message.geometry.push(preset_1_GeometryFromJSON(reader.int32()));
                        continue;
                    }
                    if (tag === 50) {
                        const end2 = reader.uint32() + reader.pos;
                        while (reader.pos < end2) {
                            message.geometry.push(preset_1_GeometryFromJSON(reader.int32()));
                        }
                        continue;
                    }
                    break;
                case 7:
                    if (tag !== 58) {
                        break;
                    }
                    const entry7 = Preset_1_TagsEntry.decode(reader, reader.uint32());
                    if (entry7.value !== undefined) {
                        message.tags[entry7.key] = entry7.value;
                    }
                    continue;
                case 8:
                    if (tag !== 66) {
                        break;
                    }
                    const entry8 = Preset_1_AddTagsEntry.decode(reader, reader.uint32());
                    if (entry8.value !== undefined) {
                        message.addTags[entry8.key] = entry8.value;
                    }
                    continue;
                case 9:
                    if (tag !== 74) {
                        break;
                    }
                    const entry9 = Preset_1_RemoveTagsEntry.decode(reader, reader.uint32());
                    if (entry9.value !== undefined) {
                        message.removeTags[entry9.key] = entry9.value;
                    }
                    continue;
                case 10:
                    if (tag !== 82) {
                        break;
                    }
                    message.fieldRefs.push(Preset_1_FieldRef.decode(reader, reader.uint32()));
                    continue;
                case 11:
                    if (tag !== 90) {
                        break;
                    }
                    message.iconRef = Preset_1_IconRef.decode(reader, reader.uint32());
                    continue;
                case 12:
                    if (tag !== 98) {
                        break;
                    }
                    message.terms.push(reader.string());
                    continue;
                case 13:
                    if (tag !== 106) {
                        break;
                    }
                    message.color = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            common: isSet(object.common) ? Common_1.fromJSON(object.common) : undefined,
            name: isSet(object.name) ? String(object.name) : "",
            geometry: Array.isArray(object === null || object === void 0 ? void 0 : object.geometry) ? object.geometry.map((e) => preset_1_GeometryFromJSON(e)) : [],
            tags: isObject(object.tags)
                ? Object.entries(object.tags).reduce((acc, [key, value]) => {
                    acc[key] = TagValue_1.fromJSON(value);
                    return acc;
                }, {})
                : {},
            addTags: isObject(object.addTags)
                ? Object.entries(object.addTags).reduce((acc, [key, value]) => {
                    acc[key] = TagValue_1.fromJSON(value);
                    return acc;
                }, {})
                : {},
            removeTags: isObject(object.removeTags)
                ? Object.entries(object.removeTags).reduce((acc, [key, value]) => {
                    acc[key] = TagValue_1.fromJSON(value);
                    return acc;
                }, {})
                : {},
            fieldRefs: Array.isArray(object === null || object === void 0 ? void 0 : object.fieldRefs)
                ? object.fieldRefs.map((e) => Preset_1_FieldRef.fromJSON(e))
                : [],
            iconRef: isSet(object.iconRef) ? Preset_1_IconRef.fromJSON(object.iconRef) : undefined,
            terms: Array.isArray(object === null || object === void 0 ? void 0 : object.terms) ? object.terms.map((e) => String(e)) : [],
            color: isSet(object.color) ? String(object.color) : "",
        };
    },
    toJSON(message) {
        var _a, _b, _c;
        const obj = {};
        if (message.common !== undefined) {
            obj.common = Common_1.toJSON(message.common);
        }
        if (message.name !== "") {
            obj.name = message.name;
        }
        if ((_a = message.geometry) === null || _a === void 0 ? void 0 : _a.length) {
            obj.geometry = message.geometry.map((e) => preset_1_GeometryToJSON(e));
        }
        if (message.tags) {
            const entries = Object.entries(message.tags);
            if (entries.length > 0) {
                obj.tags = {};
                entries.forEach(([k, v]) => {
                    obj.tags[k] = TagValue_1.toJSON(v);
                });
            }
        }
        if (message.addTags) {
            const entries = Object.entries(message.addTags);
            if (entries.length > 0) {
                obj.addTags = {};
                entries.forEach(([k, v]) => {
                    obj.addTags[k] = TagValue_1.toJSON(v);
                });
            }
        }
        if (message.removeTags) {
            const entries = Object.entries(message.removeTags);
            if (entries.length > 0) {
                obj.removeTags = {};
                entries.forEach(([k, v]) => {
                    obj.removeTags[k] = TagValue_1.toJSON(v);
                });
            }
        }
        if ((_b = message.fieldRefs) === null || _b === void 0 ? void 0 : _b.length) {
            obj.fieldRefs = message.fieldRefs.map((e) => Preset_1_FieldRef.toJSON(e));
        }
        if (message.iconRef !== undefined) {
            obj.iconRef = Preset_1_IconRef.toJSON(message.iconRef);
        }
        if ((_c = message.terms) === null || _c === void 0 ? void 0 : _c.length) {
            obj.terms = message.terms;
        }
        if (message.color !== "") {
            obj.color = message.color;
        }
        return obj;
    },
    create(base) {
        return Preset_1.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b, _c, _d, _e, _f, _g, _h;
        const message = createBasePreset_1();
        message.common = (object.common !== undefined && object.common !== null)
            ? Common_1.fromPartial(object.common)
            : undefined;
        message.name = (_a = object.name) !== null && _a !== void 0 ? _a : "";
        message.geometry = ((_b = object.geometry) === null || _b === void 0 ? void 0 : _b.map((e) => e)) || [];
        message.tags = Object.entries((_c = object.tags) !== null && _c !== void 0 ? _c : {}).reduce((acc, [key, value]) => {
            if (value !== undefined) {
                acc[key] = TagValue_1.fromPartial(value);
            }
            return acc;
        }, {});
        message.addTags = Object.entries((_d = object.addTags) !== null && _d !== void 0 ? _d : {}).reduce((acc, [key, value]) => {
            if (value !== undefined) {
                acc[key] = TagValue_1.fromPartial(value);
            }
            return acc;
        }, {});
        message.removeTags = Object.entries((_e = object.removeTags) !== null && _e !== void 0 ? _e : {}).reduce((acc, [key, value]) => {
            if (value !== undefined) {
                acc[key] = TagValue_1.fromPartial(value);
            }
            return acc;
        }, {});
        message.fieldRefs = ((_f = object.fieldRefs) === null || _f === void 0 ? void 0 : _f.map((e) => Preset_1_FieldRef.fromPartial(e))) || [];
        message.iconRef = (object.iconRef !== undefined && object.iconRef !== null)
            ? Preset_1_IconRef.fromPartial(object.iconRef)
            : undefined;
        message.terms = ((_g = object.terms) === null || _g === void 0 ? void 0 : _g.map((e) => e)) || [];
        message.color = (_h = object.color) !== null && _h !== void 0 ? _h : "";
        return message;
    },
};
function createBasePreset_1_TagsEntry() {
    return { key: "", value: undefined };
}
export const Preset_1_TagsEntry = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.key !== "") {
            writer.uint32(10).string(message.key);
        }
        if (message.value !== undefined) {
            TagValue_1.encode(message.value, writer.uint32(18).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBasePreset_1_TagsEntry();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.key = reader.string();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.value = TagValue_1.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            key: isSet(object.key) ? String(object.key) : "",
            value: isSet(object.value) ? TagValue_1.fromJSON(object.value) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.key !== "") {
            obj.key = message.key;
        }
        if (message.value !== undefined) {
            obj.value = TagValue_1.toJSON(message.value);
        }
        return obj;
    },
    create(base) {
        return Preset_1_TagsEntry.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a;
        const message = createBasePreset_1_TagsEntry();
        message.key = (_a = object.key) !== null && _a !== void 0 ? _a : "";
        message.value = (object.value !== undefined && object.value !== null)
            ? TagValue_1.fromPartial(object.value)
            : undefined;
        return message;
    },
};
function createBasePreset_1_AddTagsEntry() {
    return { key: "", value: undefined };
}
export const Preset_1_AddTagsEntry = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.key !== "") {
            writer.uint32(10).string(message.key);
        }
        if (message.value !== undefined) {
            TagValue_1.encode(message.value, writer.uint32(18).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBasePreset_1_AddTagsEntry();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.key = reader.string();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.value = TagValue_1.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            key: isSet(object.key) ? String(object.key) : "",
            value: isSet(object.value) ? TagValue_1.fromJSON(object.value) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.key !== "") {
            obj.key = message.key;
        }
        if (message.value !== undefined) {
            obj.value = TagValue_1.toJSON(message.value);
        }
        return obj;
    },
    create(base) {
        return Preset_1_AddTagsEntry.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a;
        const message = createBasePreset_1_AddTagsEntry();
        message.key = (_a = object.key) !== null && _a !== void 0 ? _a : "";
        message.value = (object.value !== undefined && object.value !== null)
            ? TagValue_1.fromPartial(object.value)
            : undefined;
        return message;
    },
};
function createBasePreset_1_RemoveTagsEntry() {
    return { key: "", value: undefined };
}
export const Preset_1_RemoveTagsEntry = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.key !== "") {
            writer.uint32(10).string(message.key);
        }
        if (message.value !== undefined) {
            TagValue_1.encode(message.value, writer.uint32(18).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBasePreset_1_RemoveTagsEntry();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.key = reader.string();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.value = TagValue_1.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            key: isSet(object.key) ? String(object.key) : "",
            value: isSet(object.value) ? TagValue_1.fromJSON(object.value) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.key !== "") {
            obj.key = message.key;
        }
        if (message.value !== undefined) {
            obj.value = TagValue_1.toJSON(message.value);
        }
        return obj;
    },
    create(base) {
        return Preset_1_RemoveTagsEntry.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a;
        const message = createBasePreset_1_RemoveTagsEntry();
        message.key = (_a = object.key) !== null && _a !== void 0 ? _a : "";
        message.value = (object.value !== undefined && object.value !== null)
            ? TagValue_1.fromPartial(object.value)
            : undefined;
        return message;
    },
};
function createBasePreset_1_FieldRef() {
    return { docId: Buffer.alloc(0), versionId: undefined };
}
export const Preset_1_FieldRef = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.docId.length !== 0) {
            writer.uint32(10).bytes(message.docId);
        }
        if (message.versionId !== undefined) {
            VersionId_1.encode(message.versionId, writer.uint32(18).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBasePreset_1_FieldRef();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.docId = reader.bytes();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.versionId = VersionId_1.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            docId: isSet(object.docId) ? Buffer.from(bytesFromBase64(object.docId)) : Buffer.alloc(0),
            versionId: isSet(object.versionId) ? VersionId_1.fromJSON(object.versionId) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.docId.length !== 0) {
            obj.docId = base64FromBytes(message.docId);
        }
        if (message.versionId !== undefined) {
            obj.versionId = VersionId_1.toJSON(message.versionId);
        }
        return obj;
    },
    create(base) {
        return Preset_1_FieldRef.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a;
        const message = createBasePreset_1_FieldRef();
        message.docId = (_a = object.docId) !== null && _a !== void 0 ? _a : Buffer.alloc(0);
        message.versionId = (object.versionId !== undefined && object.versionId !== null)
            ? VersionId_1.fromPartial(object.versionId)
            : undefined;
        return message;
    },
};
function createBasePreset_1_IconRef() {
    return { docId: Buffer.alloc(0), versionId: undefined };
}
export const Preset_1_IconRef = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.docId.length !== 0) {
            writer.uint32(10).bytes(message.docId);
        }
        if (message.versionId !== undefined) {
            VersionId_1.encode(message.versionId, writer.uint32(18).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBasePreset_1_IconRef();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.docId = reader.bytes();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.versionId = VersionId_1.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            docId: isSet(object.docId) ? Buffer.from(bytesFromBase64(object.docId)) : Buffer.alloc(0),
            versionId: isSet(object.versionId) ? VersionId_1.fromJSON(object.versionId) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.docId.length !== 0) {
            obj.docId = base64FromBytes(message.docId);
        }
        if (message.versionId !== undefined) {
            obj.versionId = VersionId_1.toJSON(message.versionId);
        }
        return obj;
    },
    create(base) {
        return Preset_1_IconRef.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a;
        const message = createBasePreset_1_IconRef();
        message.docId = (_a = object.docId) !== null && _a !== void 0 ? _a : Buffer.alloc(0);
        message.versionId = (object.versionId !== undefined && object.versionId !== null)
            ? VersionId_1.fromPartial(object.versionId)
            : undefined;
        return message;
    },
};
const tsProtoGlobalThis = (() => {
    if (typeof globalThis !== "undefined") {
        return globalThis;
    }
    if (typeof self !== "undefined") {
        return self;
    }
    if (typeof window !== "undefined") {
        return window;
    }
    if (typeof global !== "undefined") {
        return global;
    }
    throw "Unable to locate global object";
})();
function bytesFromBase64(b64) {
    if (tsProtoGlobalThis.Buffer) {
        return Uint8Array.from(tsProtoGlobalThis.Buffer.from(b64, "base64"));
    }
    else {
        const bin = tsProtoGlobalThis.atob(b64);
        const arr = new Uint8Array(bin.length);
        for (let i = 0; i < bin.length; ++i) {
            arr[i] = bin.charCodeAt(i);
        }
        return arr;
    }
}
function base64FromBytes(arr) {
    if (tsProtoGlobalThis.Buffer) {
        return tsProtoGlobalThis.Buffer.from(arr).toString("base64");
    }
    else {
        const bin = [];
        arr.forEach((byte) => {
            bin.push(String.fromCharCode(byte));
        });
        return tsProtoGlobalThis.btoa(bin.join(""));
    }
}
function isObject(value) {
    return typeof value === "object" && value !== null;
}
function isSet(value) {
    return value !== null && value !== undefined;
}
