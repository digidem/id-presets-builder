/* eslint-disable */
import _m0 from "protobufjs/minimal.js";
export const protobufPackage = "mapeo";
function createBaseVersionId_1() {
    return { coreDiscoveryKey: Buffer.alloc(0), index: 0 };
}
export const VersionId_1 = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.coreDiscoveryKey.length !== 0) {
            writer.uint32(10).bytes(message.coreDiscoveryKey);
        }
        if (message.index !== 0) {
            writer.uint32(16).int32(message.index);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseVersionId_1();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.coreDiscoveryKey = reader.bytes();
                    continue;
                case 2:
                    if (tag !== 16) {
                        break;
                    }
                    message.index = reader.int32();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            coreDiscoveryKey: isSet(object.coreDiscoveryKey)
                ? Buffer.from(bytesFromBase64(object.coreDiscoveryKey))
                : Buffer.alloc(0),
            index: isSet(object.index) ? Number(object.index) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.coreDiscoveryKey.length !== 0) {
            obj.coreDiscoveryKey = base64FromBytes(message.coreDiscoveryKey);
        }
        if (message.index !== 0) {
            obj.index = Math.round(message.index);
        }
        return obj;
    },
    create(base) {
        return VersionId_1.fromPartial(base !== null && base !== void 0 ? base : {});
    },
    fromPartial(object) {
        var _a, _b;
        const message = createBaseVersionId_1();
        message.coreDiscoveryKey = (_a = object.coreDiscoveryKey) !== null && _a !== void 0 ? _a : Buffer.alloc(0);
        message.index = (_b = object.index) !== null && _b !== void 0 ? _b : 0;
        return message;
    },
};
const tsProtoGlobalThis = (() => {
    if (typeof globalThis !== "undefined") {
        return globalThis;
    }
    if (typeof self !== "undefined") {
        return self;
    }
    if (typeof window !== "undefined") {
        return window;
    }
    if (typeof global !== "undefined") {
        return global;
    }
    throw "Unable to locate global object";
})();
function bytesFromBase64(b64) {
    if (tsProtoGlobalThis.Buffer) {
        return Uint8Array.from(tsProtoGlobalThis.Buffer.from(b64, "base64"));
    }
    else {
        const bin = tsProtoGlobalThis.atob(b64);
        const arr = new Uint8Array(bin.length);
        for (let i = 0; i < bin.length; ++i) {
            arr[i] = bin.charCodeAt(i);
        }
        return arr;
    }
}
function base64FromBytes(arr) {
    if (tsProtoGlobalThis.Buffer) {
        return tsProtoGlobalThis.Buffer.from(arr).toString("base64");
    }
    else {
        const bin = [];
        arr.forEach((byte) => {
            bin.push(String.fromCharCode(byte));
        });
        return tsProtoGlobalThis.btoa(bin.join(""));
    }
}
function isSet(value) {
    return value !== null && value !== undefined;
}
