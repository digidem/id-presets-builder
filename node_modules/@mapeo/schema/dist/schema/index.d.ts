import { type Common as _Common } from './common.js';
import { type Translation as _Translation } from './translation.js';
import { type Track as _Track } from './track.js';
import { type Role as _Role } from './role.js';
import { type ProjectSettings as _ProjectSettings } from './projectSettings.js';
import { type Preset as _Preset } from './preset.js';
import { type Observation as _Observation } from './observation.js';
import { type Icon as _Icon } from './icon.js';
import { type Field as _Field } from './field.js';
import { type DeviceInfo as _DeviceInfo } from './deviceInfo.js';
import { type CoreOwnership as _CoreOwnership } from './coreOwnership.js';
export type MapeoCommon = Simplify<_Common>;
type Simplify<T> = {
    [KeyType in keyof T]: T[KeyType];
} & {};
/** A translation is a translated message in a single language for a string property/field of any Mapeo record type */
export type Translation = Simplify<_Translation & _Common>;
/** A translation is a translated message in a single language for a string property/field of any Mapeo record type */
export type TranslationValue = Simplify<_Translation>;
/** A track is a recording of positions over time, with associated tags and attachments, similar to an observation */
export type Track = Simplify<_Track & _Common>;
/** A track is a recording of positions over time, with associated tags and attachments, similar to an observation */
export type TrackValue = Simplify<_Track>;
export type Role = Simplify<_Role & _Common>;
export type RoleValue = Simplify<_Role>;
export type ProjectSettings = Simplify<_ProjectSettings & _Common>;
export type ProjectSettingsValue = Simplify<_ProjectSettings>;
/** Presets define how map entities are displayed to the user. They define the icon used on the map, and the fields / questions shown to the user when they create or edit the entity on the map. The `tags` property of a preset is used to match the preset with observations, nodes, ways and relations. If multiple presets match, the one that matches the most tags is used. */
export type Preset = Simplify<_Preset & _Common>;
/** Presets define how map entities are displayed to the user. They define the icon used on the map, and the fields / questions shown to the user when they create or edit the entity on the map. The `tags` property of a preset is used to match the preset with observations, nodes, ways and relations. If multiple presets match, the one that matches the most tags is used. */
export type PresetValue = Simplify<_Preset>;
/** An observation is something that has been observed at a particular time and place. It is a subjective statement of 'I saw/heard this, here' */
export type Observation = Simplify<_Observation & _Common>;
/** An observation is something that has been observed at a particular time and place. It is a subjective statement of 'I saw/heard this, here' */
export type ObservationValue = Simplify<_Observation>;
/** An Icon represents metadata to retrieve an Icon blob */
export type Icon = Simplify<_Icon & _Common>;
/** An Icon represents metadata to retrieve an Icon blob */
export type IconValue = Simplify<_Icon>;
/** A field defines a form field that will be shown to the user when creating or editing a map entity. Presets define which fields are shown to the user for a particular map entity. The field definition defines whether the field should show as a text box, multiple choice, single-select, etc. It defines what tag-value is set when the field is entered. */
export type Field = Simplify<_Field & _Common>;
/** A field defines a form field that will be shown to the user when creating or editing a map entity. Presets define which fields are shown to the user for a particular map entity. The field definition defines whether the field should show as a text box, multiple choice, single-select, etc. It defines what tag-value is set when the field is entered. */
export type FieldValue = Simplify<_Field>;
export type DeviceInfo = Simplify<_DeviceInfo & _Common>;
export type DeviceInfoValue = Simplify<_DeviceInfo>;
/** Which cores belong to which identity key? */
export type CoreOwnership = Simplify<_CoreOwnership & _Common>;
/** Which cores belong to which identity key? */
export type CoreOwnershipValue = Simplify<_CoreOwnership>;
export type MapeoDoc = Translation | Track | Role | ProjectSettings | Preset | Observation | Icon | Field | DeviceInfo | CoreOwnership;
export type MapeoValue = TranslationValue | TrackValue | RoleValue | ProjectSettingsValue | PresetValue | ObservationValue | IconValue | FieldValue | DeviceInfoValue | CoreOwnershipValue;
export {};
