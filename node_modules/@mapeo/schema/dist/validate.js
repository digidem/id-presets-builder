import * as validations from './validations.js';
const validate = (schemaName, obj) => {
    const validateSchema = validations[schemaName];
    const result = validateSchema(obj);
    validate.errors = validateSchema.errors;
    return result;
};
validate.errors = null;
export { validate };
